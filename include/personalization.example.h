#pragma once
// Boot-splash personalization — TEMPLATE.
//
// Copy this file to  include/personalization.h  (which is gitignored) and set
// the recipient line below. personalization.h is NEVER committed, so the
// recipient's name stays out of this public repo (see docs/ROADMAP.md — personal
// -data audit). If personalization.h is absent, the splash uses a generic
// tagline from config.h (RECIPIENT_SPLASH default).
//
// Example:
//   #define RECIPIENT_SPLASH "Firstname · UC Davis Geology"

// #define RECIPIENT_SPLASH "Firstname · UC Davis Geology"
